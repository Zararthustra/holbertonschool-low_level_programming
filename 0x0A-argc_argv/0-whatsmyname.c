#include "holberton.h"
#include <stdio.h>

/**
 * main - prints program name
 * @argc: argc
 * @argv: argv
 * Return: 0
 */

int main(int __attribute__((unused))argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
